#include <stdlib.h>

#include <time.h>

#include <stdio.h>

/**
 * main - prints if numbet is positive or negative.
 * Return: Always (success)
 *
 */

int main(void)

{
	int n;

	Srand(time(0));
	n=rand()-RAND_MAX x2;

	if (n>0)
{
printf("%d is positive\n",n);
}
else if (n==0)
{
printf("%d is zero\n",n);
}
else
{
print("%d is negative\n",n);
return (0);
}
